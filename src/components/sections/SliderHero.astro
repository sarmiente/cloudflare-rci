---
import Left from "../../icons/leftarrow.svg";
import Right from "../../icons/rightarrow.svg";
import Chulito from "../../icons/chulito.svg";

const heroImages = [
  {
    src: "https://images.unsplash.com/photo-1676630656246-3047520adfdf?q=80&w=1158&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D",
    alt: "Security surveillance system"
  },
  {
    src: "https://images.unsplash.com/photo-1710745969844-22350d267b08?q=80&w=687&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D",
    alt: "Security control room monitoring"
  },
  {
    src: "https://images.unsplash.com/photo-1754494977436-a5c202306fe4?auto=format&fit=crop&w=1080&q=80",
    alt: "Modern security building entrance"
  },
  {
    src: "https://images.unsplash.com/photo-1558002038-1055907df827?auto=format&fit=crop&w=1080&q=80",
    alt: "Home security system smart"
  },
  {
    src: "https://images.unsplash.com/photo-1668097613572-40b7c11c8727?q=80&w=1470&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D",
    alt: "Security technology digital"
  }
];

---

<section class="relative h-screen flex items-center bg-gradient-to-br from-slate-900 to-slate-800 overflow-hidden">

  <!-- Background Images -->
  <div class="absolute inset-0">
    {heroImages.map((image, index) => (
      <div
        class="absolute inset-0 transition-opacity duration-1000 opacity-0"
        data-slide={index}
      >
        <img src={image.src} alt={image.alt} class="w-full h-full object-cover" />
      </div>
    ))}
  </div>

  <!-- Overlay -->
  <div class="absolute inset-0 bg-main/40"></div>
  <div class="absolute inset-0 bg-gradient-to-r from-main/90 to-second/50"></div>

  <!-- Navigation Arrows 
  <button
    id="prevSlide"
    class="absolute left-6 top-1/2 -translate-y-1/2 z-20 w-12 h-12 bg-white/20 backdrop-blur-sm rounded-full hidden lg:flex items-center justify-center text-white hover:bg-white/30 transition-colors group"
    aria-label="Previous image"
  >
    <Left class="w-5 h-5 " />
  </button>

  <button
    id="nextSlide"
    class="absolute right-6 top-1/2 -translate-y-1/2 z-20 w-12 h-12 bg-white/20 backdrop-blur-sm rounded-full hidden lg:flex items-center justify-center text-white hover:bg-white/30 transition-colors group"
    aria-label="Next image"
  >
    <Right class="w-5 h-5"  />
  </button>

   Slide Indicators 
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 z-20 flex gap-3">
    {heroImages.map((_, index) => (
      <button
        class="w-3 h-3 rounded-full transition-all bg-white/50 hover:bg-white/70"
        data-indicator={index}
        aria-label={`Go to slide ${index + 1}`}
      />
    ))}
  </div>
-->
  

<!-- Content -->
  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 pt-52 lg:pt-48" >
    <div class="max-w-4xl">
      <div class="flex items-center mb-6" >
        
        <h1  data-aos="fade-right"  class="text-white font-heading text-lg md:text-xl lg:text-2xl font-medium " >
          Somos Expertos en Sistemas de Seguridad
        </h1>

      </div>

      <h2  data-aos="fade-right" class=" text-4xl md:text-5xl lg:text-7xl text-white mb-6 leading-tight font-heading font-bold">
        La seguridad empieza 
        
        <span class="text-black block">por la prevención</span>
      </h2>

      <p  data-aos="fade-right" class=" md:text-xl text-white mb-6 leading-relaxed font-body">
          Instalamos soluciones modernas de seguridad que se adaptan a tus necesidades: CCTV, control de acceso, cableado estructurado y sistemas contra incendios. Protección real para cada espacio.
      </p>

      <div  data-aos="fade-up" class="flex flex-col sm:flex-row gap-4 mb-6 ">
        <button class="flex items-center bg-third hover:bg-third/60 text-white px-4 py-2 font-heading font-bold rounded-xl hover:scale-105 transition">
          <a href="https://api.whatsapp.com/send/?phone=573136775143&text=Hola%2C%20estoy%20interesado%20en%20sus%20servicios%20de%20seguridad" target="_blank">Agenda una visita</a>
          <svg
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
              class="h-4 w-4 transform transition-transform duration-300 group-hover:translate-x-1.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
            </svg>
        </button>
        
      </div>

      <div  data-aos="fade-up" class="grid grid-cols-1 sm:grid-cols-3 gap-4 font-body text-sm">
        <div  class="flex items-center gap-3">
              <Chulito class="w-5 h-5" />
              <span class="text-gray-200">Personal Tecnico Experto y Especializado</span>
          
        </div>
        <div class="flex items-center gap-3">
          <Chulito class="w-5 h-5" />
          <span class="text-gray-200">Monitoreo 24/7</span>
        </div>
        <div class="flex items-center gap-3">
          <Chulito class="w-5 h-5" />
          <span class="text-gray-200">Productos certificados</span>
        </div>
      </div>
    </div>
  </div>
</section>

<script is:inline>

  document.addEventListener("DOMContentLoaded", () => {
      let currentIndex = 0;
  const slides = document.querySelectorAll("[data-slide]");
  const indicators = document.querySelectorAll("[data-indicator]");

  function showSlide(index) {
    slides.forEach((slide, i) => {
      slide.classList.toggle("opacity-100", i === index);
      slide.classList.toggle("opacity-0", i !== index);
    });
    indicators.forEach((ind, i) => {
      ind.classList.toggle("bg-white", i === index);
      ind.classList.toggle("scale-125", i === index);
      ind.classList.toggle("bg-white/50", i !== index);
    });
    currentIndex = index;
  }

  document.getElementById("prevSlide")?.addEventListener("click", () => {
    const newIndex = currentIndex === 0 ? slides.length - 1 : currentIndex - 1;
    showSlide(newIndex);
  });

  document.getElementById("nextSlide")?.addEventListener("click", () => {
    const newIndex = currentIndex === slides.length - 1 ? 0 : currentIndex + 1;
    showSlide(newIndex);
  });

  indicators.forEach((btn, i) => {
    btn.addEventListener("click", () => showSlide(i));
  });

  setInterval(() => {
    const newIndex = currentIndex === slides.length - 1 ? 0 : currentIndex + 1;
    showSlide(newIndex);
  }, 5000);

  showSlide(0);
  })

</script>
